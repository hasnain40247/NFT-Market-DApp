service : {
  completePurchase: (principal, principal, principal) -> (text);
  getListed: () -> (vec principal) query;
  getOpenDID: () -> (principal) query;
  getOriginalOwner: (principal) -> (principal) query;
  getOwned: (principal) -> (vec principal) query;
  getPrice: (principal) -> (nat) query;
  isListed: (principal) -> (bool) query;
  listNFT: (principal, nat) -> (text);
  mint: (vec nat8, text) -> (principal);
}
